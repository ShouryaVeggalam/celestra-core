# Platform prompt library

Default product-agnostic prompts ship as in-code built-ins
(`system.assistant`, `chat.user_turn`, `analysis.summarize`).

Domain-specific sample prompts live under `prompts/examples/` and are **not**
loaded unless explicitly enabled.
